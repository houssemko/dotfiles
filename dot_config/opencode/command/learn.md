---
description: Extract a reusable pattern from this session and save it as a learned skill. Asks for confirmation before saving.
---

# Learn: $ARGUMENTS

Follow the `continuous-learning` skill. Review this session for one high-value reusable pattern (error resolution, debugging technique, workaround, project convention – never trivial typos or one-off outages).

Draft it as `name / problem / solution / example / when-to-use`, present the draft, and save to `~/.config/opencode/skills/learned/<pattern-name>/SKILL.md` only after explicit user confirmation. If nothing in the session is worth keeping, say so in one line and stop.
