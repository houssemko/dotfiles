---
description: Restate requirements, assess risks, and create a step-by-step implementation plan. Waits for user confirmation before touching any code.
---

# Plan: $ARGUMENTS

Delegate to the **planner** subagent with the request above. It will research the codebase and return a phased plan (overview, requirements, architecture changes, steps with file paths and risks, testing strategy, success criteria).

When the plan returns: present it, then STOP and wait for explicit user approval. No code changes until confirmed.
